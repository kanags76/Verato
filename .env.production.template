# ── Django ────────────────────────────────────────────────────────────────────
DJANGO_SETTINGS_MODULE=config.settings.production
DJANGO_SECRET_KEY=CHANGE_ME_use_a_long_random_string_here
DEBUG=False
ALLOWED_HOSTS=api.verato.twocents.ai
APP_BASE_URL=https://api.verato.twocents.ai

# ── CORS — add your Google AI Studio / frontend origin ───────────────────────
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com

# ── Database ──────────────────────────────────────────────────────────────────
DB_NAME=verato
DB_USER=verato
DB_PASSWORD=CHANGE_ME_strong_password_here
DB_HOST=db
DB_PORT=5432

# ── Redis / Celery ────────────────────────────────────────────────────────────
REDIS_URL=redis://redis:6379/0

# ── Google Gemini ─────────────────────────────────────────────────────────────
# Option A: API key (easiest for EC2)
GEMINI_API_KEY=CHANGE_ME
GOOGLE_CLOUD_PROJECT=verato
GOOGLE_CLOUD_LOCATION=us-central1
GEMINI_EXTRACTION_MODEL=gemini-2.5-flash-lite
GEMINI_CLASSIFY_MODEL=gemini-2.5-flash-lite
GEMINI_EMBEDDING_MODEL=text-embedding-004

# ── Email (SendGrid) ──────────────────────────────────────────────────────────
SENDGRID_API_KEY=CHANGE_ME
DEFAULT_FROM_EMAIL=noreply@verato.twocents.ai

# ── Slack ─────────────────────────────────────────────────────────────────────
SLACK_BOT_TOKEN=xoxb-CHANGE_ME
SLACK_SIGNING_SECRET=CHANGE_ME
SLACK_CLIENT_ID=CHANGE_ME
SLACK_CLIENT_SECRET=CHANGE_ME
SLACK_OAUTH_REDIRECT_URI=https://api.verato.twocents.ai/api/v1/slack/oauth/callback/

# ── File storage (leave blank to use local disk; fill in for S3) ──────────────
# AWS_S3_BUCKET_NAME=
# AWS_S3_REGION=us-east-1
