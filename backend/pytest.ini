[pytest]
DJANGO_SETTINGS_MODULE = config.settings.local
addopts = --ds=config.settings.local
python_files = tests.py test_*.py *_tests.py
markers =
    slow: LLM calls, integration tests
    unit: no DB, no network
